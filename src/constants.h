#pragma once

// constexpr int BLOCK_SIZE = 400;
constexpr int BLOCK_SIZE = 800;
// constexpr int BLOCK_SIZE = 4 * (1 << 10);

constexpr long long MIN_RID = 1;
constexpr long long MAX_RID = 1LL << 60;